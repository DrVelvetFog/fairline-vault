node_modules/
dist/
.env
*.log
.DS_Store

# Live trade logs — grow indefinitely, contain real P&L/strategy history
# simulation.json stays tracked (static backtest artifact)
logs/cycles.jsonl
logs/cycle-history.json
logs/daily-summary.json
logs/retrain-state.json

# Claude Code session data — never commit
.claude/

# One-off utility scripts
src/redeem-plp.ts

# Move build artifacts
contracts/**/build/
logs/accrual.jsonl
logs/demo-wallet.json
logs/sim-wallets.json
demo/
logs/vault-strategy.json
logs/deepbook-bm.json
contracts/**/Published.toml

# Local backups — never commit
*.old-single-tranche
logs/gate-state.json

# Faucet wallet key — never commit
logs/faucet-wallet.json

# Local Netlify folder
.netlify
